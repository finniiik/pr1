# Emoji Finder

React + TypeScript клиент и Node.js + Express API.

## Запуск сервера

```bash
cd server
npm i
node app.js
```

Сервер: http://localhost:3000/api/emojis (поиск — параметр `q`)

## Запуск клиента

```bash
npm i
npm run dev
```

Клиент: http://localhost:5173
